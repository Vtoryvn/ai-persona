{
  "name": "persona-system",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/console",
    "orchestrator",
    "deploy/persona-runner"
  ],
  "scripts": {
    "build": "npm run build -w @persona-system/shared && npm run build -w @persona-system/orchestrator && npm run build -w @persona-system/runner && npm run build -w @persona-system/console",
    "test": "npm run test --workspaces --if-present",
    "persona": "npm run start -w orchestrator --",
    "ui": "npm run start -w @persona-system/console",
    "ui:dev": "npm run dev -w @persona-system/console"
  },
  "engines": {
    "node": ">=20"
  }
}
