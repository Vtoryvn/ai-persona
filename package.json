{
  "name": "persona-system",
  "private": true,
  "workspaces": [
    "packages/shared",
    "packages/console",
    "orchestrator",
    "deploy/persona-runner"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "persona": "npm run start -w orchestrator --",
    "ui": "npm run start -w @persona-system/console",
    "ui:dev": "npm run dev -w @persona-system/console"
  },
  "engines": {
    "node": ">=20"
  }
}
